# Punto 2.6 — Auditoría de reportes "pobres" (borrador en progreso)

> Estado: COMPLETO — las 4 secciones (Gerente, Ciclista, Admin, Empleado) auditadas y confirmadas contra código y servidor real.

## Gerente — hallazgos confirmados (código + servidor real en :8007, cuenta gerente@urbanbike.com)

### /gerente/dashboard
Home/landing. 100% dataset académico Citibike (confirmado ya en 2.5). 4 KPI cards (total viajes, estaciones únicas, duración prom, distancia prom) + gráfico día-de-semana + top10 estaciones estático. Sin filtros, sin export.
Veredicto: OK como landing — el análisis profundo del mismo dataset ya vive en /gerente/analisis-citibike con filtros+paginación+export. No es redundante si se lo trata como portada.

### /gerente/analisis-citibike
Filtros (fecha, día semana, membresía, tipo bici), KPIs, gráfico DOW, tabla paginada de ~2150 estaciones, Excel + PDF export (con recorte a top100 documentado en el PDF). Ya tiene comparativas por filtro.
Veredicto: OK, ya está bien.

### /gerente/reportes
Datos reales (urbanbike_tactica.resumen_viajes_diario). Filtros fecha/membresía/tipo. 4 gráficos: top10 estaciones, DOW, duración prom por tipo bici, **tendencia diaria de viajes** (línea, ya es una serie temporal real). Tabla paginada. Excel export (el botón "Imprimir PDF" usa window.print(), no un PDF generado real — inconsistente con otros reportes que sí tienen /pdf).
Veredicto: OK, ya está bien — ya tiene tendencia real y comparativas por filtro. Gap menor: no hay comparación explícita "vs. período anterior" (%), y el export PDF es solo impresión de navegador en vez de generar_pdf_reporte como el resto. Mejora opcional, no prioritaria.

### /gerente/reportes/pagos
KPIs (ingresos del día, ingresos del mes, ticket promedio), 3 gráficos (ingresos por día del mes actual, método de pago, estado), tabla filtrable, Excel+PDF export real.
Veredicto: OK, ya está bien.

### /gerente/estrategico
Evolución mensual real (urbanbike_estrategica, precalculado por ETL): alquileres, ingresos brutos, descuentos, recargos, ganancia neta, viajes, duración prom — por mes, con filtro de rango. 2 gráficos de tendencia mensual. Excel+PDF export. Nota honesta de que "ganancia neta" no descuenta gastos reales aún (no hay datos migrados) y que "flota por mes" está pendiente por falta de historial — ambas transparentes, no ocultas.
Veredicto: Ya está bien — es exactamente el tipo de "comparativa entre períodos" que pide el punto 2.6, ya implementado.

### /gerente/informe ("Informe General")
Snapshot histórico TOTAL sin ningún filtro de fecha: total de viajes de toda la vida, tarifa promedio/hora vigente, ingresos estimados (viajes × tarifa promedio — una estimación gruesa, no ingresos reales de pagos), top5 estaciones, distribución por tipo de bicicleta y por membresía (2 donas). Sin filtros, sin paginación, sin export PDF/Excel (solo botón "Imprimir informe" = window.print()).
Veredicto: **POBRE / REDUNDANTE.** Es el reporte gerente más débil: no tiene período, no se puede comparar nada, y solapa casi totalmente con /gerente/reportes (que sí tiene fecha, filtros, tendencia y export) salvo por el desglose de membresía. "Ingresos estimados" es además una métrica floja (viajes × tarifa promedio general, ignora tarifas reales por categoría/modalidad) cuando ya existe /gerente/reportes/pagos con ingresos reales de PocketBase.
Propuesta: no es necesario rediseñarlo desde cero — dos caminos razonables: (a) eliminarlo y mover el desglose por membresía como gráfico adicional dentro de /gerente/reportes (que ya tiene fecha/filtros), o (b) si se lo mantiene como "resumen ejecutivo de una página para imprimir", agregarle período por defecto (ej. último mes cerrado) y reemplazar "ingresos estimados" por los ingresos reales ya calculados en /gerente/reportes/pagos para ese período. Esfuerzo: chico si se elige (a), mediano si se elige (b).

## Ciclista — hallazgos confirmados (servidor real en :8007, cuenta ciclista@urbanbike.com)

### /ciclista/historial (Mis Viajes) — `ciclista.py:1931`
Lista de tarjetas (43 viajes reales), una por viaje: bici, estado, ruta, fecha, duración, monto, estado de pago. Filtro por texto y estado. **Cero KPIs/agregados** (ni gasto total ni % completados/cancelados). Fuente: PocketBase `viajes`+`pagos`+`bicicletas`+`estaciones`.
Veredicto: **POBRE.** Nota: el detalle *por viaje* (qué bici, cuánto se pagó, cuántas horas) ya está cubierto por el punto **2.3** del plan ("Detalle de viaje ampliado"), separado de este punto — la propuesta acá es agregados sobre el conjunto, no el detalle de fila.
Propuesta: 2-3 KPIs arriba de la lista reusando los datos ya traídos por el handler: total gastado en el período filtrado, % completados vs cancelados, estación más usada. Sin fuente nueva.
Esfuerzo: **chico.**

### /ciclista/pagos — `ciclista.py:2106`
Tabla plana paginada (34 pagos reales) con filtros (estado, método, fecha). Sin ningún total: no suma lo pagado, no distingue pendiente/pagado/rechazado.
Veredicto: **POBRE** — exactamente el patrón "filtros + tabla, nada accionable" que describe 2.6 (el ciclista no puede ver de un vistazo "cuánto debo").
Propuesta: sobre la lista ya filtrada en el handler, sumar: total pagado en el período, total pendiente/rechazado, conteo de cargos por daños. Sin fuente nueva.
Esfuerzo: **chico.**

### /ciclista/infracciones — `ciclista.py:2658`
Tabla pura sin filtros (7 infracciones reales, todas "Resuelta" en la cuenta de prueba). Sin conteo agregado pendientes vs resueltas.
Veredicto: **POBRE**, aunque de bajo impacto (dataset por ciclista suele ser chico).
Propuesta: KPI "Pendientes: N / Resueltas: N" arriba de la tabla, dato ya presente en la lista que trae el handler.
Esfuerzo: **chico** (casi trivial).

### /ciclista/reportes — `ciclista.py:2734`
3 KPI cards (viajes realizados/completados, tiempo total en ruta, duración promedio) + gráfico de barras real "Viajes por Mes" agregado por (año, mes). Nota explícita de que no hay distancia recorrida disponible en los datos.
Veredicto: **Ya está bien** — es el único de los 4 reportes del ciclista que cumple el espíritu de 2.6 (serie temporal + KPIs, no solo listado).
Propuesta opcional (no obligatoria): sumar gasto total (de `pagos`) y cantidad de infracciones — ambos repos ya se consultan en otras rutas del mismo archivo, sin fuente nueva.
Esfuerzo: **chico-mediano** (mejora opcional, no prioritaria).

## Admin — hallazgos confirmados (servidor real en :8007, cuenta admin@urbanbike.com)

### /admin/reportes ("Reportes del Sistema") — `admin.py:992`
3 KPI cards + 3 gráficos: dona "Usuarios por Rol", barras "Bicicletas por Estado", dona "Estaciones Activas/Inactivas" — todo de PocketBase (`users`/`bicicletas`/`estaciones`). **Sin exportación Excel/PDF** (única pantalla de reportes de todo el sistema sin esa opción). Cero serie temporal, cero dato de negocio real: ignora por completo `urbanbike_operativa.facturas`, `.alquileres` y `.ordenes_mantenimiento`, que ya existen en ClickHouse y ya se consultan desde `facturas_repo.py`, `alquileres_repo.py`, `ordenes_repo.py`.
Veredicto: **POBRE — el peor caso de los 20 reportes auditados.** Se llama "Reportes del Sistema" pero solo son 3 conteos estáticos de composición, sin ningún dato accionable de negocio.
Propuesta: agregar (todo con fuentes que ya existen):
- Ingresos totales/mes desde `urbanbike_operativa.facturas` (línea/barra mensual).
- Alquileres por mes / duración promedio desde `urbanbike_operativa.alquileres`.
- Órdenes de mantenimiento abiertas vs cerradas desde `urbanbike_operativa.ordenes_mantenimiento`.
- Agregar export Excel/PDF con el patrón ya existente (`generar_excel_reporte`/`generar_pdf_reporte`).
Esfuerzo: **mediano-grande** (3 queries agregadas nuevas + export nuevo; ninguna fuente de datos es nueva).

### /admin/bitacora — `admin.py:801`
Tabla cronológica (50 registros reales): fecha, usuario, acción, detalle. Buscador client-side + export Excel/PDF. Fuente: PocketBase `bitacora_cambios`.
Veredicto: **Ya está bien tal como es un listado plano — y es correcto que lo sea.** Es un log de trazabilidad de cambios CRUD; su valor es la reconstrucción cronológica exacta, no tendencias. Forzarle KPIs sería cosmético.
Esfuerzo: N/A (opcional trivial: contador "cambios en las últimas 24h").

### /admin/auditoria — `admin.py:886`
Tabla cronológica con filtros server-side (acción, módulo, usuario) + badges por tipo (login/crear/editar/eliminar), export Excel/PDF. 100 registros reales confirmados.
Veredicto: **Ya está bien**, mismo criterio que bitácora — log de seguridad/auditoría, el listado filtrable ES la funcionalidad correcta.
Esfuerzo: N/A (opcional trivial: badge-counter por tipo de acción).

### WorkPanel de gestión de Admin (una línea cada una — no son "reportes" en el sentido de 2.6)
- `/admin/usuarios`, `/admin/bicicletas`, `/admin/estaciones`, `/admin/tarifas`: listas CRUD con filtro+paginación, sin KPIs — correcto para pantallas de gestión operativa, no aplica el criterio de 2.6.

## Empleado — hallazgos confirmados (servidor real en :8007, cuentas `empleado@urbanbike.com` [Operación], `empleado.mant@urbanbike.com` [Mantenimiento], `empleado.vig@urbanbike.com` [Vigilancia])

Los 3 sub-roles de Empleado tienen cada uno un `/dashboard` (landing con conteos/acciones pendientes, ya cubierto por el punto 1.8 — no se re-audita aquí como "reporte" salvo la parte de KPIs/tendencia real que traiga) y un `/reportes` dedicado (el análisis más profundo del rol). Confirmado leyendo `app/routers/empleado.py` completo: no hay ninguna otra pantalla de tipo reporte/dashboard en los 3 sub-roles — el resto son WorkPanels de gestión (ver abajo).

**Nota de alcance**: los puntos 2.7 (fecha límite de reparación) y 2.8 (trazabilidad de alertas/infracciones) se ejecutaron hoy mismo en sus propios worktrees hermanos, ya commiteados pero **todavía no fusionados a `main`** — esta auditoría se hizo contra el código real de `main` tal como está hoy (sin esas mejoras todavía), que es lo correcto dado que no están fusionadas. `/mantenimiento/ordenes`, `/vigilancia/alertas` y `/vigilancia/infracciones` son WorkPanels de gestión, no "reportes" en el sentido de 2.6, así que esto no cambia el veredicto de ninguna pantalla de esta sección.

### Operación

#### /operacion/dashboard
Landing: 4 conteos de flota (disponible/en uso/mantenimiento/retirada) vía gráfico de barras + 1 tarjeta de acción pendiente ("Cobros pendientes de verificar", punto 1.8). Sin filtros, sin export, sin serie temporal.
Veredicto: OK como landing — mismo criterio que los dashboards de Gerente/Admin ya evaluados: el análisis profundo real vive en `/operacion/reportes`, no aquí.

#### /operacion/reportes
Datos reales (`alquileres_repo.listar`, `urbanbike_operativa`). Filtros (estado, fecha desde/hasta), 2 KPI cards (alquileres filtrados, monto total de la página), tabla paginada de alquileres reales, Excel+PDF export. Además, una segunda tabla real ("Bicicletas por Categoría y Estado") con su propio Excel+PDF, sin relación con el filtro de fecha de arriba.
Veredicto: **ACEPTABLE, con el mismo hueco que 2.6 señala en general — cero visualización de tendencia.** A diferencia de `/gerente/reportes` (que sí tiene un gráfico de línea de tendencia diaria), aquí todo es tabla plana + KPIs puntuales, pese a que el propio filtro de fecha ya habilita comparar rangos manualmente. La segunda tabla ("Bicicletas por Categoría y Estado") es una foto estática del catálogo, no relacionada con el rango de fechas filtrado — es información de operación válida, pero no es del mismo tema que la tabla de alquileres de arriba en la misma pantalla, lo cual es un poco confuso.
Propuesta: agregar un gráfico de línea/barras "Alquileres por día" sobre el mismo rango ya filtrado (los datos ya están en el handler, no hace falta fuente nueva). Esfuerzo: **chico**.

### Mantenimiento

#### /mantenimiento/dashboard
Landing: conteo de bicicletas en mantenimiento por tipo (clásica/eléctrica) + 2 tarjetas de acción pendiente ("Mantenimientos activos", "Órdenes por actualizar", punto 1.8). Sin filtros, sin export, sin serie temporal.
Veredicto: OK como landing, mismo criterio que el resto de dashboards.

#### /mantenimiento/reportes
**POBRE — y además con un dato real incorrecto, no solo un problema de diseño.** Consulta la colección `ordenes_mant` de PocketBase (`empleado.py:2758`) — la misma colección que la sección 83 de `docs/HOJA_DE_RUTA.md` ya documentó como **huérfana** (desactualizada, reemplazada por `ordenes_repo`/ClickHouse como única fuente real de órdenes desde el punto 1.8 Parte 1, y reafirmado explícitamente en el punto 2.7 de hoy: "`ordenes_mant` de PocketBase NO se toca, NO se reactiva"). Verificado en vivo contra el servidor real: la tarjeta "Total de órdenes" muestra **6** (leído directo del HTML de `/empleado/mantenimiento/reportes`), mientras que `ordenes_repo.listar()` — la fuente real que usa el resto de Mantenimiento, incluida `/mantenimiento/ordenes` — tiene **13** órdenes reales hoy. Es decir, este reporte le falta más de la mitad de las órdenes reales, sin ningún aviso. Además: 2 gráficos (dona de estado, barras de tipo preventivo/correctivo) sobre ese dato obsoleto, sin filtros, sin ningún export Excel/PDF (única pantalla de reportes de todo el sistema auditado en 2.6 sin ninguna opción de exportar).
Propuesta: (a) migrar la fuente de datos a `ordenes_repo` (mismo patrón que ya usa `/mantenimiento/ordenes` y `/mantenimiento/dashboard`) — esto solo es correcto hacerlo, no opcional, dado que hoy muestra un número real activamente engañoso; (b) agregar filtro de fecha/prioridad y export Excel/PDF, siguiendo el patrón ya establecido en el resto de reportes del sistema; (c) considerar agregar una serie temporal de órdenes abiertas/cerradas por semana o mes. Esfuerzo: **mediano** (el fix de la fuente de datos es chico y urgente por sí solo; agregar filtros+export+tendencia es el resto del esfuerzo).

### Vigilancia

#### /vigilancia/dashboard
Landing: 3 tarjetas de acción pendiente (Seguimientos activos, Devoluciones por validar, Reparaciones por certificar, punto 1.8) + KPIs reales de ClickHouse (viajes, retrasos, duración promedio) + top10 estaciones por viajes/retrasos (gráfico de barras). Nota: los KPIs de ClickHouse están fijados a un solo día del dataset académico (`2023-10-31`, mismo dataset y misma limitación ya señalada para Gerente en la sección de arriba — no es un bug de este punto, es la naturaleza del dataset Citibike usado en todo el sistema).
Veredicto: OK — es de hecho el dashboard más completo de los 3 sub-roles de Empleado (único con un mini "top10" real, no solo conteos).

#### /vigilancia/reportes
**POBRE**, el mismo patrón "listado agregado sin filtros, sin export, sin comparativa" que ya describe 2.6 en general. Consulta viajes reales completados (`estado="completado"`, 42 viajes reales confirmados en esta auditoría) y calcula un split binario "a tiempo" (≤60 min) vs "tardías" (>60 min) — un solo gráfico de dona. Sin filtro de fecha (agrega el histórico completo de una sola vez, no hay forma de ver "esta semana" vs "el mes pasado"), sin exportación Excel/PDF (mismo hueco que Mantenimiento).
Propuesta: agregar filtro de rango de fechas (mismo patrón que `/operacion/reportes`) + Excel/PDF export + una segunda vista con el detalle real de duración por viaje (hoy solo hay un conteo binario, sin ver la distribución real de minutos). Esfuerzo: **chico-mediano**.

### WorkPanels de gestión de Empleado (no son "reportes" en el sentido de 2.6)
- Operación: `/operacion/inventario`, `/operacion/alquileres`, `/operacion/rebalanceo`, `/operacion/pagos` — listas CRUD/acción con filtro+paginación y export propio, correcto para pantallas operativas.
- Mantenimiento: `/mantenimiento/ordenes`, `/mantenimiento/bicicletas` — ídem (`/mantenimiento/ordenes` recibió mejoras reales hoy vía el punto 2.7, en worktree separado, aún sin fusionar).
- Vigilancia: `/vigilancia/seguimiento`, `/vigilancia/devoluciones`, `/vigilancia/infracciones`, `/vigilancia/mantenimiento/cerrar`, `/vigilancia/alertas`, `/vigilancia/soporte` — ídem (`/vigilancia/alertas` e `/vigilancia/infracciones` recibieron mejoras reales hoy vía el punto 2.8, en worktree separado, aún sin fusionar).

